# PLAN.md — GSend

> 本文件是 2026-08-31 一次完整需求訪談(grilling)後的共識基準。之後每個開發 session 以此為準;要推翻任何一條決策,先更新本文件並記下原因。

## 1. 一句話定位

開網頁就能用的一次性跨裝置檔案/文字傳輸工具:兩台裝置以 4 位數字碼配對,瀏覽器對瀏覽器直連傳輸,伺服器不經手、不儲存任何內容。

## 2. 產品定義(已定案)

| 項目 | 決策 |
|---|---|
| 核心功能 | 一次性檔案傳送 + 雙向文字框 |
| 拓撲 | 1 對 1,單一 session |
| Session 模型 | 配對一次後保持連線,期間可來回傳多個檔案與文字,任一方關頁即結束 |
| 配對方式 | 4 位數字碼(60 秒失效)或 QR code(掃描自動導向網址並連接) |
| 傳送確認 | 接收方連上後,傳送方畫面顯示已連線,按「開始」才傳(防數字碼被猜中冒充) |
| 帳號 | 無帳號、無資料庫、無個資 |
| 文字框 | 無紀錄,session 結束即消失,零儲存 |
| 檔案上限 | 全平台 1GB;僅在 OPFS 不可用時(如 Safari 無痕模式)降為 256MB。**原訂「Safari 200MB」已作廢,見 §10** |
| 多檔案 | 支援多選;v1 不支援資料夾遞迴 |
| 續傳 | **支援跨斷線續傳**(見 §4 設計後果) |

## 3. 技術路線(已定案)

### 3.1 傳輸協定:WebRTC(裸 API),不用 iroh

原始構想是沿用 Dashbeam 的 iroh/iroh-blobs,經查證後**放棄**,理由(已核實於 iroh 官方文件,2026-08):

- 瀏覽器裡的 iroh **只能全程走 relay**(WebSocket 到 relay 伺服器),打洞直連在瀏覽器不可行——iroh 的核心賣點在 web 版完全用不上。
- 瀏覽器裡的 iroh-blobs 只有記憶體儲存(MemStore),檔案大小受瀏覽器記憶體限制。
- Rust→WASM 工具鏈對單人零經驗開發者成本過高。
- WebRTC 反而能做到真正的瀏覽器↔瀏覽器直連,且為全平台協定:未來 Android/iOS App 用各平台原生 WebRTC 庫即可與瀏覽器互通。**協定一旦選定即全平台通用,不可混用(WebRTC 端與 iroh 端互相講不通)。**

實作層:**裸 RTCPeerConnection + DataChannel + 自寫信令**,不用 PeerJS/simple-peer 等封裝庫(維護狀況參差,出問題卡在黑盒外;裸 API 每層可診斷)。

### 3.2 技術棧

- 前端:**React + Vite + TypeScript**(AI 輔助開發語料最豐富的生態)
- 網站託管:**Cloudflare Pages**
- 信令伺服器:**Cloudflare Workers + Durable Objects**(WebSocket,管理數字碼→配對;免費層起步)
- TURN:**v1 不設**。打洞失敗顯示明確錯誤(建議兩裝置連同一 Wi-Fi),信令端記匿名失敗計數,用實測數據決定日後是否加限額 TURN
- PWA:v1 之後補(vite-plugin-pwa)

### 3.3 支援瀏覽器

Chrome / Edge / Android Chrome + Safari / iOS Safari 全列必須;Safari 走 200MB 降級路徑;Firefox 盡力而為。

## 4. 續傳的設計後果(第一天就要納入,不是事後補)

- 4 位數字碼 60 秒即失效,**斷線重連不能再走數字碼**:配對成功時兩端交換並保留 session 金鑰(存記憶體/localStorage),信令伺服器允許憑金鑰重新加入原 session。
- chunk 協定從第一天就設計為可續傳:固定 chunk 大小、完成表(bitmap)、重連握手時比對已完成 chunk 再續傳,配校驗。

## 5. 平台路線圖

1. **Web v1**(本計畫,三個月)
2. **PWA**(可安裝、Android 分享選單)
3. **原生 App(Android → iOS)**:推遲到有真實用戶需求證據(系統分享選單、背景傳輸)才做;屆時沿用 WebRTC 協定

## 6. 營運與發布

- **開源**:GitHub 公開 repo,MIT License
- **產品名稱**:GSend(2026-08-31 定案)
- **網域**:待購。接收方要在另一台裝置手動輸入網址,所以網址本身就是 UX 的一部分,要短而好唸——**這是目前唯一未決事項**
- **UI 語言**:英文為主 + 繁體中文切換
- **預算**:上限 USD $50/月(Cloudflare 免費層 + 網域年費,實際幾乎用不到)
- **隱私**:上線前寫簡單隱私說明頁(伺服器僅經手信令,不經手、不儲存檔案內容)

## 7. 三個月里程碑

> 紀律:**每週至少部署一次到正式環境**。單人專案最大死因是悶頭做三個月卡死在上線。

### M1(週 1–4)· 通道打通
- 第 1 週就完成 Cloudflare 部署(最大未知先拆掉)
- 數字碼配對信令(Durable Objects + WebSocket)
- 裸 WebRTC DataChannel 建連
- Chrome↔Chrome 傳成一個小檔;傳送方確認流程
- chunk 協定按可續傳設計(§4)
- **驗收:自己的電腦↔手機,用正式網址傳成一張照片**

### M2(週 5–8)· 產品化
- 1GB 分塊傳輸 + 進度條 + 背壓處理
- 多檔案、雙向文字框
- **跨斷線續傳**(session 金鑰重配對 + chunk 完成表)
- 打洞失敗的明確錯誤路徑
- **驗收:iPhone Safari↔Windows Chrome 互傳成功** ✅ 2026-08-31 真機通過

### M3(週 9–12)· 公測打磨
- Safari 降級路徑細節(200MB 上限、串流寫入替代方案)
- 匿名失敗率計數、隱私說明頁、英文+繁中雙語
- 找 5 個朋友在不同網路環境實測、修 bug、正式上線
- **驗收:一個沒見過你的人,靠網址自己完成一次傳輸**

### 時程風險
續傳排入後 M2 很滿。若進度吃緊,先把 Safari 打磨挪到 M3,**不砍每週部署、不砍續傳**(已明確選擇保留)。

## 8. 訪談中已否決的選項(避免日後重新爭論)

- Fork Dashbeam(AGPL-3.0 綁定 + 繼承陌生 codebase)→ 只參考架構,不抄程式碼
- iroh/iroh-blobs 路線 → §3.1 理由
- 帳號系統、附近裝置探索(瀏覽器無 mDNS)→ v1 只做分享碼
- 持續資料夾同步(Syncthing 型)→ 是另一個產品,不做
- TURN 兜底 → v1 不設,數據說話
- PeerJS 等封裝庫 → 用裸 API
- VPS 自架 → Cloudflare 免維運

## 9. 實作進度

### 已完成(M1 主體,2026-08-31)

專案骨架建立於 `C:\Project\GSend`,技術棧與 §3.2 一致,已本機端對端驗證:

- Worker 信令伺服器:4 位數字碼配置(以 Durable Object 名稱作為路由位址)、60 秒有效期、
  首次配對後燒毀、per-IP 限速(12 次/分)、5 分鐘 resume 寬限期、閒置回收。
- 裸 WebRTC:perfect negotiation、ctrl/data 雙通道(控制訊息不被大檔阻塞)、
  ICE restart、STUN only(無 TURN)。
- 傳輸引擎:分塊 + 背壓(8MB/1MB 水位)、進度 ack、取消、**續傳協定(offset 交換)**。
- 介面:建立/加入、QR 深層連結(`/?c=1234`)、倒數、**傳送方確認閘門**、
  雙向檔案傳輸、無紀錄文字框、進度條、錯誤訊息。

驗證結果:Chrome↔Chrome 雙向傳輸,2MB 與 32MB 檔案皆逐位元組零誤差;文字通道正常;
`code_not_found` 等錯誤路徑顯示正確訊息。

### 對 §3.2 的一處調整

原訂「Cloudflare Pages + Workers」改為 **Workers with static assets**(單一 Worker
同時服務靜態網站與 `/api/ws`)。理由:同源、單一 `npm run deploy`、少一個平台要學,
對單人開發是嚴格的簡化,能力上完全等價。

### M1 剩餘工作

- 真實網域部署與手機實測(M1 驗收條件:自己的電腦↔手機用正式網址傳成一張照片)。
- 網域尚未購買,是唯一的阻塞項(產品名稱已定為 GSend)。

### M1 之後已完成

- **重載韌性**(2026-08-31):session 憑證存入 sessionStorage;以每次載入的實例 id
  區分「網路抖動」與「對方重新載入」,前者重啟 ICE 保留傳輸、後者重建 PeerConnection;
  client 重連預算由 75 秒改為對齊伺服器的 5 分鐘;對方久未回來時明確結束並說明原因。
  起因是真機 iOS Safari 上分頁跑掉後就再也接不回來。
- **匿名連線指標**:每個 session 記錄一筆「是否連上、走區網還是穿透 NAT」,
  伺服器端去重,只留每日總數,公開於 `/api/stats`。這是 M3 決定要不要付費用 TURN 的證據來源。
- **串流寫入**:接收端改用 OPFS(dedicated worker + sync access handle),
  上限提升到 1GB,不再受分頁記憶體限制。

- **重載續傳**:接收端在傳輸中途重新載入後可以接續。中繼資料隨 sessionStorage 一起保存,
  重載後重開 OPFS 檔案並以**檔案本身的大小**作為續傳點(不信任任何記憶中的計數,
  所以不會飄移)。實測:128MB 檔案在 19MB 處重載接收端,完成後逐位元組零誤差。
  傳送端重載時則明確告知對方停止等待(見下)。

  過程中修掉兩個只有在真實重連才會出現的競態:被丟棄的 PeerConnection 其
  `close` 事件會延遲送達,把剛接上的新通道拆掉;以及傳送迴圈抓著舊通道物件,
  在一個永遠不會觸發的 `bufferedamountlow` 上無限等待。

- **背景切換的可見性修正**(真機 iPhone Safari 回報):切走分頁十秒再回來,信令連線會斷,
  重連時 `onHello` 無條件把階段打回 `pairing`,導致畫面顯示「Almost there」而傳輸清單整個消失
  ——但底下的 WebRTC 通道其實還活著,檔案仍在傳、也確實傳完了,使用者只是看不到。
  重新整理後檔案更是直接不見(已完成的接收沒有被持久化)。三處修正:
  信令重連不再降級已建立的階段;只要 session 已批准就一直顯示傳輸清單,不因連線品質而隱藏;
  **已完成但尚未儲存的接收也會持久化**,重載後從 OPFS 重建下載連結。

### 已知缺口

- 傳送端永遠無法在自身重載後續傳——瀏覽器不允許頁面重載後繼續讀取先前選的檔案,
  這是平台規則不是缺陷。現在會明確通知接收端「傳送端已重載,無法繼續」,而不是讓它卡住。
- 尚未使用 `showSaveFilePicker()`(Chromium 桌面與 Chrome Android 132+ 可直接串流到
  使用者選定位置)。Safari 與 Firefox 沒有 picker,blob 路徑無論如何都要保留。
- 尚未做 PWA 與中英切換。

## 10. 對已定案決策的修正

### Safari 200MB 上限(原 §2)已作廢

Q12/Q21 當時決定「Chrome 1GB、Safari 200MB」,依據是「iOS Safari 每個來源約 1GB 配額」
這個廣泛流傳的說法。查證後發現**該說法自 Safari 17 / iOS 17(2023)起已經過時**:
現在每個來源可得約磁碟總量的 60%,且不再跳出配額詢問視窗。web.dev 上仍在流傳的
「1GB 起、每次增加 200MB」是 iOS 17 之前的舊制;MDN 與 WebKit 官方儲存政策公告都已更新。

因此 Safari 不需要特別降級,全平台統一 1GB。真正需要降級的情況不是「Safari」,
而是「OPFS 不可用」(主要是無痕模式),那時退回 256MB 的記憶體路徑。

### 2026-08-31 修正:QR 加入時檔案上限錯誤

真機回報:Safari 收 285MB 檔案被拒,顯示 "too large for the receiving device"。

原因是儲存能力探測 `prepareStorage()` 只在 Landing 畫面被呼叫,而**透過 QR code 或連結加入時
Landing 根本不會渲染**。於是接收端檢查大小時,`diskAvailable` 仍是未知狀態,
`maxFileBytes()` 保守地退回 256MB 的記憶體上限,285MB 就被判定超標。

修正:大小檢查前先等探測完成;探測改在應用啟動時就發動,不再依賴特定畫面;
拒絕訊息改為附上實際上限,讓下次若再誤判能直接看出是哪個數字在作用。

### 2026-08-31 修正:重連後對方卡在「Waiting for the other device」

真機回報:明明兩邊都連著,其中一邊卻停在等待對方的畫面。

根因在信令伺服器。`openResume` 會先關掉被取代的舊 socket,再送出 `peer-resumed`。
但舊 socket 的 close 事件是**非同步**的,稍後才觸發 `handleDetach`,無條件再送一個
`peer-left`。對方收到的順序因此是「對方回來了」→「對方離開了」,而且之後不會再有任何
訊息來澄清,於是永遠停在等待畫面。這在每次對方靜默斷線重連時都會發生
(iOS 把分頁凍結正是這種)。

修正:`handleDetach` 只在該角色**沒有其他存活 socket** 時才通報離開。
另外加了兩層防線:資料通道一旦開啟就清除「對方離開」狀態(通道開著就是對方在的證明);
介面顯示等待訊息前也要求通道確實未開。

同時修掉一個相關缺陷:同一個實例重連時,原本只有連線狀態剛好是 `failed`/`reconnecting`
才會嘗試修復,連線已 `closed` 就沒人重建,session 會永遠停在 Connecting。
現在改由 `revivePeer()` 統一判斷:通道已開就不動,連線已死就重建,其餘嘗試 ICE restart。

### M2 驗收通過(2026-08-31,真機)

iPhone Safari ↔ Windows Chrome 雙向互傳成功,258MB 檔案完成,**手機端傳輸中途重新整理
也能續收**。這是 §7 M2 的驗收條件,至此 M2 的清單全部完成:
1GB 串流寫入、多檔案、雙向文字框、跨斷線續傳、打洞失敗錯誤路徑。

前一輪的困惑值得記下:接收端顯示的是舊版錯誤訊息,因為那台裝置的頁面是部署前載入的。
`index.html` 是 `max-age=0, must-revalidate`,重新整理即可取得新版,但已開啟的分頁不會自動更新。
**日後修正接收端行為後,務必兩台都重新整理再測**,否則會誤判修正無效。

### 首批連線指標

`/api/stats` 累積 4 筆,全部是 `connectedInternet`(穿透 NAT 成功),零失敗。
樣本太小不足以決定 TURN,但至少證明指標管線在真實環境運作正常。
