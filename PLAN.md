# PLAN.md — GSend

> 本文件是 2026-08-31 一次完整需求訪談(grilling)後的共識基準。之後每個開發 session 以此為準;要推翻任何一條決策,先更新本文件並記下原因。

## 1. 一句話定位

開網頁就能用的一次性跨裝置檔案/文字傳輸工具:兩台裝置以 4 位數字碼配對,瀏覽器對瀏覽器直連傳輸,伺服器不經手、不儲存任何內容。

## 2. 產品定義(已定案)

| 項目 | 決策 |
|---|---|
| 核心功能 | 一次性檔案傳送 + 雙向文字框 |
| 拓撲 | 1 對 1,單一 session |
| Session 模型 | 配對一次後保持連線,期間可來回傳多個檔案與文字,任一方關頁即結束 |
| 配對方式 | 4 位數字碼(60 秒失效)或 QR code(掃描自動導向網址並連接) |
| 傳送確認 | **已作廢,見 §17。**改為:先選檔案再產碼,對方輸入碼即開始接收;傳送方即時看到對方進來並可隨時中止 |
| 帳號 | 無帳號、無資料庫、無個資 |
| 文字框 | 無紀錄,session 結束即消失,零儲存 |
| 檔案上限 | **原訂固定 1GB 已作廢,見 §17。**現為:直寫使用者資料夾時無上限(只受磁碟空間限制);走 OPFS 時由配額推導(桌機通常數 GB);OPFS 不可用時(如 Safari 無痕模式)降為 256MB |
| 多檔案 | 支援多選;v1 不支援資料夾遞迴 |
| 續傳 | **支援跨斷線續傳**(見 §4 設計後果) |

## 3. 技術路線(已定案)

### 3.1 傳輸協定:WebRTC(裸 API),不用 iroh

原始構想是沿用 Dashbeam 的 iroh/iroh-blobs,經查證後**放棄**,理由(已核實於 iroh 官方文件,2026-08):

- 瀏覽器裡的 iroh **只能全程走 relay**(WebSocket 到 relay 伺服器),打洞直連在瀏覽器不可行——iroh 的核心賣點在 web 版完全用不上。
- 瀏覽器裡的 iroh-blobs 只有記憶體儲存(MemStore),檔案大小受瀏覽器記憶體限制。
- Rust→WASM 工具鏈對單人零經驗開發者成本過高。
- WebRTC 反而能做到真正的瀏覽器↔瀏覽器直連,且為全平台協定:未來 Android/iOS App 用各平台原生 WebRTC 庫即可與瀏覽器互通。**協定一旦選定即全平台通用,不可混用(WebRTC 端與 iroh 端互相講不通)。**

實作層:**裸 RTCPeerConnection + DataChannel + 自寫信令**,不用 PeerJS/simple-peer 等封裝庫(維護狀況參差,出問題卡在黑盒外;裸 API 每層可診斷)。

### 3.2 技術棧

- 前端:**React + Vite + TypeScript**(AI 輔助開發語料最豐富的生態)
- 網站託管:**Cloudflare Pages**
- 信令伺服器:**Cloudflare Workers + Durable Objects**(WebSocket,管理數字碼→配對;免費層起步)
- TURN:**v1 不設**。打洞失敗顯示明確錯誤(建議兩裝置連同一 Wi-Fi),信令端記匿名失敗計數,用實測數據決定日後是否加限額 TURN
- PWA:v1 之後補(vite-plugin-pwa)

### 3.3 支援瀏覽器

Chrome / Edge / Android Chrome + Safari / iOS Safari 全列必須;Safari 走 200MB 降級路徑;Firefox 盡力而為。

## 4. 續傳的設計後果(第一天就要納入,不是事後補)

- 4 位數字碼 60 秒即失效,**斷線重連不能再走數字碼**:配對成功時兩端交換並保留 session 金鑰(存記憶體/localStorage),信令伺服器允許憑金鑰重新加入原 session。
- chunk 協定從第一天就設計為可續傳:固定 chunk 大小、完成表(bitmap)、重連握手時比對已完成 chunk 再續傳,配校驗。

## 5. 平台路線圖

1. **Web v1**(本計畫,三個月)
2. **PWA**(可安裝、Android 分享選單)
3. **原生 App(Android → iOS)**:推遲到有真實用戶需求證據(系統分享選單、背景傳輸)才做;屆時沿用 WebRTC 協定

## 6. 營運與發布

- **開源**:GitHub 公開 repo,MIT License
- **產品名稱**:GSend(2026-08-31 定案)
- **網域**:已購入並上線於 **gsend.cc**(2026-08-31)。接收方要在另一台裝置手動輸入網址,所以短而好唸本身就是 UX 的一部分
- **UI 語言**:英文為主 + 繁體中文切換
- **預算**:上限 USD $50/月(Cloudflare 免費層 + 網域年費,實際幾乎用不到)
- **隱私**:上線前寫簡單隱私說明頁(伺服器僅經手信令,不經手、不儲存檔案內容)

## 7. 三個月里程碑

> 紀律:**每週至少部署一次到正式環境**。單人專案最大死因是悶頭做三個月卡死在上線。

### M1(週 1–4)· 通道打通
- 第 1 週就完成 Cloudflare 部署(最大未知先拆掉)
- 數字碼配對信令(Durable Objects + WebSocket)
- 裸 WebRTC DataChannel 建連
- Chrome↔Chrome 傳成一個小檔;傳送方確認流程
- chunk 協定按可續傳設計(§4)
- **驗收:自己的電腦↔手機,用正式網址傳成一張照片**

### M2(週 5–8)· 產品化
- 1GB 分塊傳輸 + 進度條 + 背壓處理
- 多檔案、雙向文字框
- **跨斷線續傳**(session 金鑰重配對 + chunk 完成表)
- 打洞失敗的明確錯誤路徑
- **驗收:iPhone Safari↔Windows Chrome 互傳成功** ✅ 2026-08-31 真機通過

### M3(週 9–12)· 公測打磨
- Safari 降級路徑細節(200MB 上限、串流寫入替代方案)
- 匿名失敗率計數、隱私說明頁、英文+繁中雙語
- 找 5 個朋友在不同網路環境實測、修 bug、正式上線
- **驗收:一個沒見過你的人,靠網址自己完成一次傳輸**

### 時程風險
續傳排入後 M2 很滿。若進度吃緊,先把 Safari 打磨挪到 M3,**不砍每週部署、不砍續傳**(已明確選擇保留)。

## 8. 訪談中已否決的選項(避免日後重新爭論)

- Fork Dashbeam(AGPL-3.0 綁定 + 繼承陌生 codebase)→ 只參考架構,不抄程式碼
- iroh/iroh-blobs 路線 → §3.1 理由
- 帳號系統、附近裝置探索(瀏覽器無 mDNS)→ v1 只做分享碼
- 持續資料夾同步(Syncthing 型)→ 是另一個產品,不做
- TURN 兜底 → v1 不設,數據說話
- PeerJS 等封裝庫 → 用裸 API
- VPS 自架 → Cloudflare 免維運

## 9. 實作進度

### 已完成(M1 主體,2026-08-31)

專案骨架建立於 `C:\Project\GSend`,技術棧與 §3.2 一致,已本機端對端驗證:

- Worker 信令伺服器:4 位數字碼配置(以 Durable Object 名稱作為路由位址)、60 秒有效期、
  首次配對後燒毀、per-IP 限速(12 次/分)、5 分鐘 resume 寬限期、閒置回收。
- 裸 WebRTC:perfect negotiation、ctrl/data 雙通道(控制訊息不被大檔阻塞)、
  ICE restart、STUN only(無 TURN)。
- 傳輸引擎:分塊 + 背壓(8MB/1MB 水位)、進度 ack、取消、**續傳協定(offset 交換)**。
- 介面:建立/加入、QR 深層連結(`/?c=1234`)、倒數、**傳送方確認閘門**、
  雙向檔案傳輸、無紀錄文字框、進度條、錯誤訊息。

驗證結果:Chrome↔Chrome 雙向傳輸,2MB 與 32MB 檔案皆逐位元組零誤差;文字通道正常;
`code_not_found` 等錯誤路徑顯示正確訊息。

### 對 §3.2 的一處調整

原訂「Cloudflare Pages + Workers」改為 **Workers with static assets**(單一 Worker
同時服務靜態網站與 `/api/ws`)。理由:同源、單一 `npm run deploy`、少一個平台要學,
對單人開發是嚴格的簡化,能力上完全等價。

### M1 剩餘工作

- 真實網域部署與手機實測(M1 驗收條件:自己的電腦↔手機用正式網址傳成一張照片)。
- 網域尚未購買,是唯一的阻塞項(產品名稱已定為 GSend)。

### M1 之後已完成

- **重載韌性**(2026-08-31):session 憑證存入 sessionStorage;以每次載入的實例 id
  區分「網路抖動」與「對方重新載入」,前者重啟 ICE 保留傳輸、後者重建 PeerConnection;
  client 重連預算由 75 秒改為對齊伺服器的 5 分鐘;對方久未回來時明確結束並說明原因。
  起因是真機 iOS Safari 上分頁跑掉後就再也接不回來。
- **匿名連線指標**:每個 session 記錄一筆「是否連上、走區網還是穿透 NAT」,
  伺服器端去重,只留每日總數,公開於 `/api/stats`。這是 M3 決定要不要付費用 TURN 的證據來源。
- **串流寫入**:接收端改用 OPFS(dedicated worker + sync access handle),
  上限提升到 1GB,不再受分頁記憶體限制。

- **重載續傳**:接收端在傳輸中途重新載入後可以接續。中繼資料隨 sessionStorage 一起保存,
  重載後重開 OPFS 檔案並以**檔案本身的大小**作為續傳點(不信任任何記憶中的計數,
  所以不會飄移)。實測:128MB 檔案在 19MB 處重載接收端,完成後逐位元組零誤差。
  傳送端重載時則明確告知對方停止等待(見下)。

  過程中修掉兩個只有在真實重連才會出現的競態:被丟棄的 PeerConnection 其
  `close` 事件會延遲送達,把剛接上的新通道拆掉;以及傳送迴圈抓著舊通道物件,
  在一個永遠不會觸發的 `bufferedamountlow` 上無限等待。

- **背景切換的可見性修正**(真機 iPhone Safari 回報):切走分頁十秒再回來,信令連線會斷,
  重連時 `onHello` 無條件把階段打回 `pairing`,導致畫面顯示「Almost there」而傳輸清單整個消失
  ——但底下的 WebRTC 通道其實還活著,檔案仍在傳、也確實傳完了,使用者只是看不到。
  重新整理後檔案更是直接不見(已完成的接收沒有被持久化)。三處修正:
  信令重連不再降級已建立的階段;只要 session 已批准就一直顯示傳輸清單,不因連線品質而隱藏;
  **已完成但尚未儲存的接收也會持久化**,重載後從 OPFS 重建下載連結。

### 已知缺口

- 傳送端永遠無法在自身重載後續傳——瀏覽器不允許頁面重載後繼續讀取先前選的檔案,
  這是平台規則不是缺陷。現在會明確通知接收端「傳送端已重載,無法繼續」,而不是讓它卡住。
- ~~尚未使用 `showSaveFilePicker()`~~ **已於 §17 實作**(改用 `showDirectoryPicker()`,
  一次選一個資料夾涵蓋整個 session)。Safari 與 Firefox 沒有 picker,OPFS 路徑仍保留。
- 尚未做 PWA 與中英切換。

## 10. 對已定案決策的修正

### Safari 200MB 上限(原 §2)已作廢

Q12/Q21 當時決定「Chrome 1GB、Safari 200MB」,依據是「iOS Safari 每個來源約 1GB 配額」
這個廣泛流傳的說法。查證後發現**該說法自 Safari 17 / iOS 17(2023)起已經過時**:
現在每個來源可得約磁碟總量的 60%,且不再跳出配額詢問視窗。web.dev 上仍在流傳的
「1GB 起、每次增加 200MB」是 iOS 17 之前的舊制;MDN 與 WebKit 官方儲存政策公告都已更新。

因此 Safari 不需要特別降級,全平台統一 1GB。真正需要降級的情況不是「Safari」,
而是「OPFS 不可用」(主要是無痕模式),那時退回 256MB 的記憶體路徑。

### 2026-08-31 修正:QR 加入時檔案上限錯誤

真機回報:Safari 收 285MB 檔案被拒,顯示 "too large for the receiving device"。

原因是儲存能力探測 `prepareStorage()` 只在 Landing 畫面被呼叫,而**透過 QR code 或連結加入時
Landing 根本不會渲染**。於是接收端檢查大小時,`diskAvailable` 仍是未知狀態,
`maxFileBytes()` 保守地退回 256MB 的記憶體上限,285MB 就被判定超標。

修正:大小檢查前先等探測完成;探測改在應用啟動時就發動,不再依賴特定畫面;
拒絕訊息改為附上實際上限,讓下次若再誤判能直接看出是哪個數字在作用。

### 2026-08-31 修正:重連後對方卡在「Waiting for the other device」

真機回報:明明兩邊都連著,其中一邊卻停在等待對方的畫面。

根因在信令伺服器。`openResume` 會先關掉被取代的舊 socket,再送出 `peer-resumed`。
但舊 socket 的 close 事件是**非同步**的,稍後才觸發 `handleDetach`,無條件再送一個
`peer-left`。對方收到的順序因此是「對方回來了」→「對方離開了」,而且之後不會再有任何
訊息來澄清,於是永遠停在等待畫面。這在每次對方靜默斷線重連時都會發生
(iOS 把分頁凍結正是這種)。

修正:`handleDetach` 只在該角色**沒有其他存活 socket** 時才通報離開。
另外加了兩層防線:資料通道一旦開啟就清除「對方離開」狀態(通道開著就是對方在的證明);
介面顯示等待訊息前也要求通道確實未開。

同時修掉一個相關缺陷:同一個實例重連時,原本只有連線狀態剛好是 `failed`/`reconnecting`
才會嘗試修復,連線已 `closed` 就沒人重建,session 會永遠停在 Connecting。
現在改由 `revivePeer()` 統一判斷:通道已開就不動,連線已死就重建,其餘嘗試 ICE restart。

### M2 驗收通過(2026-08-31,真機)

iPhone Safari ↔ Windows Chrome 雙向互傳成功,258MB 檔案完成,**手機端傳輸中途重新整理
也能續收**。這是 §7 M2 的驗收條件,至此 M2 的清單全部完成:
1GB 串流寫入、多檔案、雙向文字框、跨斷線續傳、打洞失敗錯誤路徑。

前一輪的困惑值得記下:接收端顯示的是舊版錯誤訊息,因為那台裝置的頁面是部署前載入的。
`index.html` 是 `max-age=0, must-revalidate`,重新整理即可取得新版,但已開啟的分頁不會自動更新。
**日後修正接收端行為後,務必兩台都重新整理再測**,否則會誤判修正無效。

### 首批連線指標

`/api/stats` 累積 4 筆,全部是 `connectedInternet`(穿透 NAT 成功),零失敗。
樣本太小不足以決定 TURN,但至少證明指標管線在真實環境運作正常。

## 11. M3 進度

### 已完成(2026-09-01)

- **PWA 基礎**:web app manifest、自製產生的圖示(含 Android 用的 maskable 版本)、
  精簡的 service worker。可加入主畫面或從網址列安裝。
  對 iOS 的實際意義不只是圖示:已安裝的副本擁有自己的儲存待遇,而不是與 Safari 共用,
  收到的檔案正是存在那裡等你儲存。

  service worker 刻意寫得很窄:**文件一律走網路優先,只有帶內容雜湊的建置產物才走快取**。
  這是刻意的——我們剛在 8/31 吃過「接收端跑著舊頁面」的虧,不能讓安裝後的副本卡在舊版。

- **隱私說明頁**(`/privacy`,英文與繁中雙語):說明伺服器經手什麼、
  永遠收不到什麼、什麼留在你的裝置上,以及還有誰參與(代管商,以及建立連線時
  會得知 IP 位址的 STUN 伺服器)。最後一項是容易被略過但必須誠實揭露的。

圖示以 `scripts/make-icons.mjs` 直接對 zlib 編碼 PNG 產生,沒有引入影像處理相依套件;
改圖後執行 `npm run icons`。

- **介面中英雙語**(2026-09-01):依瀏覽器語言自動偵測,頁首可切換,選擇存於 localStorage。
  英文字典的鍵定義型別、中文字典必須完整,**漏翻譯會編譯失敗**而不是上線後才被發現。

  一個設計上的必要決定:兩台裝置可能使用不同語言,所以**傳輸中止的原因改用代碼傳輸**
  (原本是英文句子),由看畫面的那一方自己決定措辭。實測:從英文端取消,
  中文端顯示「傳送方已取消」,英文端顯示 "Cancelled"。

- **Android 分享選單**(share target,2026-09-01):已安裝的副本會出現在系統「分享到」清單。
  分享的 POST 由 service worker 攔下、把檔案暫存在 Cache Storage,再導回應用取回。
  分享等於明確的傳送意圖,所以應用會**立刻建立 session 並顯示數字碼**,
  把檔案押著直到對方被同意為止,而不是丟使用者到一個空白畫面。
  實測:multipart 表單導航 → 攔截 → 自動開 session → 對方加入並同意 → 檔案零誤差送達,
  分享的標題文字也一併變成訊息送出。僅 Chromium 支援;Safari 沒有 share target。

### M3 剩餘

- 找 5 個朋友在不同網路環境實測 ← **這是最後一項**,也是累積 TURN 決策樣本的唯一途徑。

### 新發現的小缺口

大檔傳輸途中按取消,對方要幾秒後才反應。控制訊息雖走獨立通道,但與資料共用同一條
SCTP 連線的壅塞視窗,會排在已送出的資料之後。傳送端會立即停止,接收端稍後才跟上。
若要改善,方向是把背壓水位從 8 MiB 調低,代價是吞吐略降——需要先量測再決定。

## 12. 真機回報與修正(2026-09-01)

### 已修

- **一方結束連線,另一方持續空轉**:伺服器原本只是把 socket 關掉(code 4003),
  而 client 會忽略 4000 段的關閉碼(那是為了「已另外送過錯誤訊息」的情況設計的),
  結果對方永遠不知道發生什麼事。現在結束前會先送一則帶原因代碼的訊息,
  對方立刻顯示「另一台裝置結束了這個連線」。
- **傳送中狀態誤報「重新連線中」**:WebRTC 的 `disconnected` 只代表「短時間沒收到封包」,
  在行動網路上很常見而且通常會自己恢復。原本一收到就改狀態,讓正常的傳輸看起來像壞掉。
  現在要持續 4 秒才會回報。
- **失敗次數嚴重低估**:原本只有在 ICE 正式判定 `failed` 後再等 15 秒才記一筆,
  但使用者早在那之前就按了結束連線。現在**放棄等待也會記一筆失敗**——
  那正是最該被計入的一種失敗,也是 TURN 決策依據的數字。

### 未解:跨網路打洞失敗(這是 TURN 的訊號)

真機回報:iPhone(行動網路)↔ 電腦(Wi-Fi)反覆卡在連線中;
Android ↔ 電腦(不同網路)第一次成功、第二次失敗、重試才成功。

這是典型的行動網路 CGNAT / 對稱式 NAT 症狀。**只有 STUN 的情況下無解**,
因為對稱式 NAT 會為每個目的地開不同的對外埠,打洞前提不成立。這不是 bug,
是 Q17「v1 不設 TURN」這個決定在真實世界的代價。

決策依據還在累積(修正計數低估後數字才會誠實)。若要解,方向是加一台 TURN;
需要先查清楚目前的方案與費用,再對照每月 USD $50 的預算上限。

## 13. TURN 決策(2026-09-01):加,用 Cloudflare Realtime

Q17 當初決定「v1 不設 TURN,用數據決定」。數據到齊了:跨行動網路的連線基本上必然失敗,
因為電信業者的 CGNAT / 對稱式 NAT 讓打洞的前提不成立。這不是機率問題,不需要更多樣本。
而「電腦傳給手機」正是這個工具最核心的用途。

**選定 Cloudflare Realtime TURN**:$0.05/GB,每月 1,000 GB 免費額度,而且已經在 Cloudflare 上。
以每次 1GB、完全走中轉計,約每月 1,000 次才開始收費;之後 $50 預算還能再買約 1TB。
自架 coturn 在 Hetzner 上的流量單價更好(20TB/€6),但要自己顧 TLS 續期與埠段,
**只有用超過 1TB/月才值得**,屆時再換。

### 為什麼風險低

ICE 的優先權公式(RFC 8445)給中轉候選的偏好值是 0、直連是 126,
**中轉在設計上就是最後手段**。打洞成功的連線根本不會用到它,也就不會產生費用。
這不是改成中轉架構,而是加一張只在打洞失敗時才展開的安全網。

### 實作要點

- 憑證由 **DO 在已認證的信令連線上發放**,不另開公開端點——那會變成任何人都能消耗額度的洞。
- 一個 session 只鑄一組憑證,兩端共用,存活 2 小時。
- API 金鑰只存在 Worker secret,**永遠不進瀏覽器**。
- 未設定金鑰時自動退回純 STUN,行為與加入 TURN 之前完全一致(已實測)。
- 指標新增 `connectedRelay`,用來看實際用掉多少免費額度。

### 尚待啟用

需要在 Cloudflare 儀表板的 Realtime → TURN 建立金鑰,然後設定兩個 secret
(`TURN_KEY_ID`、`TURN_KEY_API_TOKEN`)。程式碼已就緒且已部署。

## 14. TURN 已啟用,但有一個未解的成本問題(2026-09-01)

### 已可用

Cloudflare Realtime TURN 已上線並驗證:憑證由 Worker 鑄造成功,瀏覽器拿到完整中轉清單,
包含 `turns:turn.cloudflare.com:443`(穿透防火牆的路徑)。`/api/relay` 可隨時檢查狀態。

設定過程的兩個坑值得記下:`wrangler secret put` 的輸入是隱藏的,貼上很容易只進去一個字元
(實際發生了,兩個 secret 都只有 1 字元);改用儀表板設定並按 **Deploy**(不是 Save version)才生效。
另外 `/api/relay` 一開始沒設 `Cache-Control: no-store`,瀏覽器快取住舊的失敗回應,
一度誤導判斷——狀態端點被快取是很糟的陷阱。

### 未解:直連可行時仍然走中轉

實測:同一台機器上的兩個分頁,直連明明可行,**每一次都被 ICE 選了中轉**,
等 5 秒穩定後重讀仍是中轉(所以不是量測競態)。後果是 6MB 要 9~40 秒(加 TURN 前是每秒數 MB),
而且每條連線都在消耗免費額度。

推測原因:Chrome 的 mDNS 主機候選需要解析時間,而 TURN 配置完成得快,
中轉配對先通過連通性檢查就被提名,之後不會再換。RFC 8445 的優先權只決定**同時完成檢查者**的提名順序,
不保證時間上的後備。

**嘗試過的修法失敗了**:第一次連線只給 STUN、失敗才升級到中轉。結果連線完全建立不起來
(ICE 一直停在檢查中、從不回報 failed,所以升級邏輯也never觸發),已回退。

可行的方向(尚未實作):
- 在建立連線前先本機探測 NAT 型別,只有對稱式 NAT 才附上 TURN
- 或提供中轉憑證但延後 500ms~1s 再加入,讓直連配對先完成檢查
- 或接受現況,靠 Cloudflare 的計量與 1,000GB 免費額度撐著,先觀察真實用量

在解決之前,**請留意 `/api/stats` 的 `connectedRelay`**:它現在會反映真實的額度消耗速度。

## 15. §14 的問題已解決,且原診斷需要更正(2026-09-01)

### 解法:限時的兩階段 ICE

第一次連線只帶 STUN;**6 秒內通道沒開啟(或 ICE 明確失敗)**,就用
`setConfiguration` + ICE restart 把中轉加進**同一條連線**(不重建)。
直連候選仍會繼續被檢查,能直連時依然直連——這把「中轉是最後手段」從優先權層面
落實到了時間層面。上次失敗的「等 failed 才升級」行不通,因為 ICE 會停在 checking
永遠不報 failed;**期限是唯一保證會觸發的訊號**。

實測(儀器化驗證):pc 以 3 台純 STUN 建立 → 只蒐集到 host 候選 → 6 秒觸發
`setConfiguration servers=4` → 蒐集到 5 個 relay 候選 → connected → 6MB 零誤差。

途中修掉一個自己寫出的 bug:憑證請求的 3 秒保底逾時會**無條件覆寫** ICE 伺服器快照,
把真回覆蓋成純 STUN,讓升級誤以為無可升級。Promise 有防重入、快照沒有——現在只認第一次交付。

另加 `?relay=force` 除錯參數(強制 relay-only policy),可在任何網路上驗證中轉是否可用。

### 原診斷更正:「中轉搶走直連」並未被證實

§14 寫「直連可行時仍走中轉」。儀器化之後發現真相:**這台開發機目前整個 UDP 都被擋**
——所有 STUN(含 Google)都回 701,連 mDNS 都不通,兩個分頁之間根本不存在直連路徑。
所以當時「每次都走中轉」是中轉在正確地做它唯一能做的事,不是搶。
慢(6MB 花 40 秒)是 SCTP-over-TCP/TLS 中轉的固有代價,不是 bug。

「相同機器兩分頁卻無直連」這個反直覺現象,正是當初 iPhone/Android 跨網路
時好時壞的另一面:這台機器的網路環境本身就是「嚴格防火牆」測試場。
限時升級的設計在這種環境下升級救場、在正常網路下保護速度與額度,兩種情況都對。

## 16. 視覺改版:Classical(2026-09-01)

依 Claude Design 專案《GSend 新設計》全面改版,設計系統為 classical-5de54cc4:

- **語言**:紙面底(#f3f2f2)、墨色字(#201f1d)、金銅點綴(#b68235)、16% 髮絲線分隔。
  標題 Cormorant Garamond、內文 Lora、中文襯線 Noto Serif TC;所有資料一律等寬字。
- **進度條走 CLI**:等寬 `█ ░` 方塊字元 + 百分比 + 速度 / eta / 位元組(速度與 ETA
  在前端由進度差分以 EMA 平滑計算,引擎不變)。取消的傳輸剩餘格改畫 `╳`。
- **版面**:單欄置中 1040px,欄內以 1px 髮絲線分左右(首頁 1fr/380px、傳輸 1fr/300px,
  右欄 sticky);≤860px 收成單欄,按鈕最小 48px 高。
- **細節**:輸入格 4 個獨立方格 + 閃爍游標(真正的 input 隱形覆蓋其上,行動鍵盤與
  自動填入不受影響);數字碼大格用襯線齊線數字;倒數改為 2px 細條;mono 狀態區塊
  (route/joined、peer/limit/code、stun/turn)刻意不翻譯,是設計的 CLI 語氣;
  頁首狀態 chip 顯示「已連線 · direct/turn」(Snapshot 新增 relayEngaged)。
- **深色模式**:設計只定義紙面淺色,深色由同一套語言推導(暖墨底 #1c1a18、
  提亮的金 #cf9a4d),隱私頁與 manifest/theme-color/圖示一併換色。
- **順手修正**:隱私頁仍寫著「本專案沒有中轉伺服器」——TURN 上線後已不真實,
  中英兩版都改為誠實揭露(中繼只經手密文,看得到 IP 與流量、看不到內容)。

驗證:桌機首頁/數字碼/批准卡/傳輸帳本(96MB 實傳,速度與 eta 即時顯示)、
手機斷點、深色模式皆與設計對齊;型別檢查通過後部署。

## 17. 流程反轉與直寫磁碟(2026-09-04)

起點是一個問題:1GB 的上限有沒有辦法突破。答案是有,而且 1GB 從來不是技術上限
——WebRTC DataChannel 對總傳輸量沒有任何限制,那個數字只是 `sink.ts` 裡寫死的常數。
真正的天花板一直在「接收端怎麼把位元組落地」。因此這次同時改了兩件事:落地方式,
以及為了讓落地方式行得通而必須調整的流程順序。

### 17.1 落地改用 File System Access(原 §9「已知缺口」第二項)

接收端在**按下「加入」的那一下**呼叫 `showDirectoryPicker()`,拿到資料夾 handle 後,
每個 chunk 透過 `FileSystemWritableFileStream` 以 `position` 直接寫進目的檔案。
中間不再有任何緩衝副本,所以:上限等於剩餘磁碟空間、沒有兩倍磁碟消耗、
傳完就已經存好,不需要再按一次儲存。

**為什麼一定要綁在那一下**:picker 需要 transient user activation,而 activation
只有幾秒壽命,撐不到 WebRTC 連上、offer 送達之後。等到知道檔案清單再問已經太遲。
QR / 連結加入的路徑沒有任何點擊可以借,所以改在連線等待畫面上放一顆「選擇儲存資料夾」
——連線本來就要花一兩秒,那段時間正好拿來問;不選就退回瀏覽器儲存,不擋流程。

**代價要記下來**:Chrome 的 writable stream 是寫在 swap 檔上、`close()` 才提交。
因此斷線可以復原(stream 活得比連線久),但**頁面重載無法復原**——半個檔案不會留下。
持久化紀錄多了一個 `savedAs` 欄位就是為了這件事:帶著它的紀錄可以重新顯示,
但絕不會被當成可續傳的對象。已完成的那一筆重載後仍會回到清單上,只是顯示「已存檔」
而不是一顆按了會壞掉的儲存鍵。

**檔名要當成不可信輸入**。OPFS 路徑一律用不透明 id 命名,所以從來不需要處理;
直寫路徑則是把對方送來的字串寫進使用者真實的資料夾,因此 `safeName()` 會剝掉路徑分隔符、
控制字元、Windows 非法字元、開頭的點與結尾的點/空白,並擋掉 CON/PRN 這類保留名。
同名不覆蓋,改加 `(2)` 後綴。已用 `../../..\evil<na*me>.txt` 實測:寫成
`evil_na_me_.txt` 且留在選定資料夾內,沒有任何東西逸出。

**沒有 picker 的瀏覽器**(Firefox、Safari)維持 OPFS 路徑,但上限不再寫死:
改由 `navigator.storage.estimate()` 推導(扣掉 256MB 餘裕,夾在 256MB 與 64GB 之間)。
實測桌機 Chrome 顯示 4.9GB,遠高於原本的 1GB。

**順手修掉一個真的 bug**:`sendFiles()` 原本用**自己這台**的接收上限去過濾要送出的檔案。
傳送端是從磁碟讀,根本不佔儲存空間,這個檢查沒有意義——一台 Safari 無痕模式的手機
會拒絕送出 500MB 的檔案,即使對方有 2TB 空間。上限只由接收端把關(`onOffer` 裡本來就是對的)。

### 17.2 流程反轉:先選檔案,再產碼,對方輸入即收(作廢原 §2「傳送確認」)

原流程是「建立連線 → 對方加入 → 傳送方按同意 → 才開始傳」。新流程是
「選檔案 → 產生 4 位數字碼 → 對方輸入碼即開始接收 → 兩邊保持連線繼續互傳」。
`approve`/`reject` 兩個控制訊息、`Approval` 狀態與批准畫面全部移除,
開啟資料通道本身就是唯一的閘門。原本只有系統分享選單走的「先押著檔案、連上再送」
路徑,現在成為首頁的主路徑(`host(files, text)`)。

**這確實讓掉一道防線,而且要誠實記下來。**原本四道防護(60 秒失效、首次即燒毀、
per-IP 限速、傳送方批准)少了最後一道:猜中數字碼的人不再是「連上但拿不到東西」,
而是直接拿到佇列裡的東西。4 位數 = 10,000 組,分散式猜測在 60 秒窗口內打得穿。

已評估過改成 6 位數(keyspace 提高到 1,000,000,可完整補回),**但決定維持 4 位**
——4 位數是這個產品的識別特徵,而且 QR / 連結路徑本來就不用打字。
補償措施是把「批准」換成「即時可中止」:傳送方在對方一連上就看到明確提示與中止鍵,
可以在傳輸中途砍掉。這是自覺的取捨,不是疏漏;若日後真的出現濫用證據,
提高位數是現成的解法。

### 17.3 驗證

兩個分頁實測(把 picker 換成真實的 OPFS 資料夾 handle,走的是同一條 DiskSink 程式碼):
5MB 檔案傳完後逐位元組比對零誤差、檔名正確、狀態列顯示 `saving <資料夾>` 與 `limit 磁碟`;
反向再傳三個檔案驗證同名後綴與檔名淨化;拔掉 picker 驗證 OPFS 退路(顯示推導出的 4.9GB
上限與儲存鍵);接收端重載驗證「已存檔」紀錄回得來且不誤判為可續傳。
